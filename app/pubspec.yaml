name: tagsy_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.6
  flutter_rust_bridge: 2.12.0
  # Annotations for the freezed sealed unions the flutter_rust_bridge codegen
  # emits for Rust enums-with-data (e.g. ApiEvent, OperationStatusDto). The
  # generated `api.dart` imports this and declares `part 'api.freezed.dart'`,
  # which `build_runner` fills in from the `freezed` dev-dependency below.
  freezed_annotation: ^2.4.4
  # Read/write JSON annotations that freezed's generated code references.
  json_annotation: ^4.9.0
  # Receives files/text shared to the app via the Android share sheet
  # (ACTION_SEND / ACTION_SEND_MULTIPLE). Handles the intent plumbing,
  # content-resolver reads, and cold/warm-start cases.
  # Pinned to 1.8.x: 1.9.0 requires Kotlin 2.4.0 / compileSdk 37, but this
  # project builds with Kotlin 2.2.20 (android/settings.gradle.kts). 1.8.1
  # exposes the same Dart API (getMediaStream / getInitialMedia / reset).
  receive_sharing_intent: ">=1.8.1 <1.9.0"
  # Outgoing share: hands a file to the Android share sheet (the reverse of
  # receive_sharing_intent). Used by the file detail screen's share button.
  # On Android it registers its own FileProvider via manifest merger, so no
  # manual <provider> is needed here.
  share_plus: ^10.1.4
  flutter_markdown_plus: ^1.0.3
  # Renders the real bytes of an SVG file inline once tap-to-fetch has pulled
  # them (SvgPicture.file), the vector counterpart to Image.file for rasters.
  # The daemon still generates the small SVG *thumbnail* (via resvg) shown until
  # the full file arrives; this renders the fetched file at full fidelity. The
  # two rasterizers can differ on exotic SVG features, same trade-off as
  # markdown (daemon snippet vs. rich local render).
  flutter_svg: ^2.0.10+1
  # HSV color wheel + alpha slider + hex input for the tag style editor's color
  # fields. Purely a UI convenience: it works in `Color`, which we round-trip
  # through the `#RRGGBBAA` strings the core stores (parseTagColor / the editor's
  # hex controllers). No backend/protocol/schema involvement.
  flutter_colorpicker: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Code generation for the freezed sealed unions in the generated Rust
  # bindings. `dart run build_runner build` produces `api.freezed.dart`.
  build_runner: ^2.4.13
  freezed: ^2.5.7

  # Generates launcher icons for each platform from a single source PNG.
  # Run with `dart run flutter_launcher_icons` (or `flutter pub run
  # flutter_launcher_icons`) after changing `assets/icon.png`. Config lives
  # in the `flutter_launcher_icons:` section below.
  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  image_path: "assets/icon.png"

  android: true
  min_sdk_android: 21
  # Adaptive icon (Android 8+). The launcher masks the foreground into a
  # circle/squircle/etc., so keep the logo inside the inner ~66% of the
  # canvas. Using the same 1024x1024 as the foreground for now; swap in a
  # padded version if the logo gets clipped.
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/icon.png"

  linux:
    generate: true
    image_path: "assets/icon.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
